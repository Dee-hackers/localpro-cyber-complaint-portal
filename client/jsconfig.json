{
    "compilerOptions": {
        "baseUrl": ".",
        "paths": {
            "@/*": [
                "./src/*"
            ]
        },
        "checkJs": false // Disable type checking for JavaScript files
    },
    "exclude": [
        "node_modules" // Exclude node_modules to avoid unnecessary type errors
    ]
}